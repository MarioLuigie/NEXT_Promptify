import '@/styles/globals.css'

export const metadata = {
  title: 'Promptify',
  description: 'AI application for using prompts'
}

export default function RootLayout({ children }) {
	return <div>{children}</div>
}
